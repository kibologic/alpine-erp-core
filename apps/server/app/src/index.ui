// alpine-erp-core no longer mounts its own shell.
// Modules are registered into alpine-shell via register.ui.
// This file is intentionally empty.
console.log('[erp-core] standing by — loaded by alpine-shell');
